extends layout

block content
    h1= title
    h4 Join Game
    p Game ID:
    textarea(id="gameId" placeholder="Enter a game id")
    br
    p Name:
    textarea(id="playerName" placeholder="Enter your name")
    //- button(class='game-button' onclick="newGame()") Submit
    
    form(action="/game" onclick="joinGame()")
        input(type="submit" value="Submit")